apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sidan-gov-api.fullname" . }}-test-connection"
  labels:
    {{- include "sidan-gov-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sidan-gov-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
